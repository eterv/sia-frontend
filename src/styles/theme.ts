// 색상 이름은 임의대로 하였지만,
// Design System이 있다면 정확한 정의 및 사용을 할 수 있음

export const colors = {
  //primary: 'blue',
};

export const theme = {
  colors,
};

export type Colors = typeof colors;
export type Theme = typeof theme;
